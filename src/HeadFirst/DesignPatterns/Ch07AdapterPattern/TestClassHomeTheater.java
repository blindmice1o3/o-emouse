package HeadFirst.DesignPatterns.Ch07AdapterPattern;

public class TestClassHomeTheater {
    public static void main(String[] args) {
        /*
        // instantiate components here.

    // Here we're creating the components right in the test drive. Normally the client is given a facade, it doesn't have
    // to construct one itself.

        // First you instantiate the Facade with all the components in the subsystem.
        HomeTheaterFacade homeTheater = new HomeTheaterFacade(amp, tuner, dvd, cd, projector, screen, lights, popper);

        // Use the simplifed interface to first start the movie up, and then shut it down.
        homeTheater.watchMovie("Raiders of the Lost Ark");
        homeTheater.endMovie();
        */
    }
}
